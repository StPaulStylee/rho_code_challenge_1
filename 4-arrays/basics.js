// 1 - Create a variable 'arr' that is assigned the value of an empty array.

// 2 - Create a variable 'arr1' that is assigned the value of an array with the values 2, 3, 8, and 10.

// 3 - Console log the value of arr1 at index 2.

//4 - Console log the length of both arrays.

arr = [];
arr1 = [2, 3, 8, 10];
console.log(arr1[2]);
console.log(arr.length);
console.log(arr1.length);
